{
  "buildCommand": "npm run build",
  "installCommand": "npm install && pip3 install -r requirements.txt",
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
